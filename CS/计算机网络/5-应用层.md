# 计算机网络：应用层

## C/S 和 P2P 模型

## DNS

即 Domain Name System，域名系统

## FTP

即 File Transfer Protocol 文件传输协议

## 电子邮件

协议包括 SMTP、POP3、IMAP 等，同用户代理 UA、邮件服务器共同构成完整的电子邮件系统

## HTTP

WWW 即 World Wide Web，万维网。核心由下面三个标准构成：

- 统一资源定位符 URL
- 超文本传输协议 HTTP
- 超文本标记语言 HTML

HTTP 即 HyperText Transfer Protocol，超文本传输协议，是 WWW 的核心协议，定义了客户端和服务器之间的通信规则和数据格式

### HTTP 的请求和响应

HTTP 请求由请求行、请求头、空行和请求体组成。HTTP 响应由状态行、响应头、空行和响应体组成。

HTTP 请求方法包括 GET、POST、PUT、DELETE 等，分别用于不同的操作类型。

HTTP 状态码包括 1xx（信息性状态码）、2xx（成功状态码）、3xx（重定向状态码）、4xx（客户端错误状态码）和 5xx（服务器错误状态码）等，分别表示不同的响应结果。

以下是一个简单的 HTTP 请求和响应示例：

```text
GET /index.html HTTP/1.1
Host: www.example.com
HTTP/1.1 200 OK
Content-Type: text/html
<html>
  <body>
    <h1>Hello, World!</h1>
  </body>
</html>
```

