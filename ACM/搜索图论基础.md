---
标签:
  - 图论
  - 搜索
难度:
  - ★
---
### 本章内容

- 深度优先搜索——DFS

- 广度优先搜索——BFS

- 图的存储：边列表、邻接矩阵、邻接表

- 树与图的遍历：找树重心、拓扑排序

- 最短路：Dijkstra、Bellman、SPFA、Floyd

- 最小生成树：Prim、Kruskal

- 二分图：染色法、匈牙利算法

> [!info] Category:算法  
>  
> [https://zh.wikipedia.org/wiki/Category:算法](https://zh.wikipedia.org/wiki/Category:算法)  

### 图论相关概念

> [!info] 图论相关概念 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oiwiki.org/graph/concept/](https://oiwiki.org/graph/concept/)  

# 一、DFS

> [!info] DFS（搜索） - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oi-wiki.org/search/dfs/](https://oi-wiki.org/search/dfs/)  

|   |   |   |   |   |   |
|---|---|---|---|---|---|
||数据结构|空间需求|区别|适用于|重点|
|DFS|stack|O (n)|不具有最短性|思路复杂的，最空间要求高的|回溯和减枝|
|BFS|queue|O (2^n)|最短路特性|最短最少最小||

### DFS 例题 1 全排列问题

> [!info] 全排列问题 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1706](https://www.luogu.com.cn/problem/P1706)  

```C++
\#include <iostream>
\#include <vector>
using namespace std;
int n,sta[15];
vector<bool> flag(15,0);
void dfs(int depth){
    if(depth>=n){
        for(int i=0;i<n;i++)
            printf("%5d",sta[i]);
        printf("\n");
        return;
    }
    for(int i=1;i<=n;i++){
        if(flag[i]==0){
            flag[i]=1;
            sta[depth]=i;
            dfs(depth+1);
            flag[i]=0;
        }
    }
    return;
}
int main(){
    cin>>n;
    dfs(0);
}
```

- DFS 如果要记录路径，可以将 `flag` 数组声明为`int`类型并初始化为`-1` ，在递归中记录`depth`

### DFS 例题 2 八皇后

> [!info] [USACO1.5] 八皇后 Checker Challenge - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1219](https://www.luogu.com.cn/problem/P1219)  

思路 1，定义`map[][]`数组，一个一个格子进行DFS，需要传递 `x`，`y` 作为遍历深度/位置。

思路 2，由于每行仅仅能存放一个皇后，可以将问题转化为数组全排列问题，比如`a[3]=6`代表存放在第三行第六列

两种思路都需要存放三种条件，分别是：列是否被使用`ifUsed`，左斜对角线是否被使用`ifUsedL`，右斜对角线是否被使用`ifUsedR`。对于思路2`ifUsedL`和`ifUsedR`的使用相当于剪枝，不需要枚举完所有的情况再判断是否有对角线冲突，而对于思路1，三个数组都是剪枝。

以下代码使用思路 2`initialize()`函数用于事先计算某行某列属于第几个左右对角线，减少DFS内部判断

```C++
\#include <iostream>
\#include <vector>
using namespace std;
const int MAX=13;
int n;
vector<bool> ifUsed(MAX+1,0);
vector<bool> ifUsedL(MAX*2,0);
vector<bool> ifUsedR(MAX*2,0);
int lline[MAX+1][MAX+1];
int rline[MAX+1][MAX+1];
void initialize(){
    for(int i=1;i<=n;i++){
        for(int j=1;j<=n;j++){
            lline[i][j]=n+i-j;
            rline[i][j]=i+j-1;
        }
    }
}
int current[MAX+1];
int ptime=3;
int ans=0;
void dfs(int layer){
    if(layer>n){
        if(ptime>0){
            for(int i=1;i<=n;i++)
                cout<<current[i]<<' ';
            cout<<endl;
            ptime--;
        }
        ans++;
        return;
    }
    for(int i=1;i<=n;i++){
        if (ifUsed[i]==0 &&
            ifUsedL[lline[layer][i]]==0 &&
            ifUsedR[rline[layer][i]]==0){
            ifUsed[i]=ifUsedL[lline[layer][i]]=ifUsedR[rline[layer][i]]=1;
            current[layer]=i;
            dfs(layer+1);
            ifUsed[i]=ifUsedL[lline[layer][i]]=ifUsedR[rline[layer][i]]=0;
        }
    }
    return;
}
int main() {
    cin>>n;
    initialize();
    dfs(1);
    cout<<ans<<endl;
}
```

剪枝有两种，可行性剪枝与最优性剪枝，上述剪枝属于可行性剪枝

# 二、BFS

> [!info] BFS（搜索） - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oi-wiki.org/search/bfs/](https://oi-wiki.org/search/bfs/)  

- Dp 是一种无环最短路问题，最短路问题包含了 Dp

BFS 运算时使用的数据结构是队列，在求最短路问题中时，先将起点入队，开始迭代时，先将队头出队，然后将符合结果的点入队，当对列为空时停止循环

BFS 与 DFS 都是一种思想，没有固定的框架，以下时 BFS 的经典例题

### BFS 例题 1 马的遍历

> [!info] 马的遍历 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1443](https://www.luogu.com.cn/problem/P1443)  

```C++
\#include <iostream>
\#include <cstring>
\#define f first
\#define s second
using namespace std;
const int N=410;
int map[N][N];
int n,m,x,y;
// use [1][1] as point (1,1)
pair<int,int> q[N*N];
void bfs(){
    map[x][y]=0;
    q[0].f=x;
    q[0].s=y;
    int qhead=0,qtail=0;
    int xmove[8]={2,2,-2,-2,1,-1,1,-1};
    int ymove[8]={1,-1,1,-1,2,2,-2,-2};
    while(qhead<=qtail){
        pair<int,int> tmp=q[qhead++];
        for(int i=0;i<8;i++){
            int x=tmp.f+xmove[i];
            int y=tmp.s+ymove[i];
            if(x<=n&&x>=1&&y<=m&&y>=1&&map[x][y]==-1){
                map[x][y]=map[tmp.f][tmp.s]+1;
                q[++qtail].first=x;
                q[qtail].second=y;
            }
        }
    }
    for(int i=1;i<=n;i++){
        for(int j=1;j<=m;j++)
            printf("%-5d",map[i][j]);
        printf("\n");
    }
}
int main() {
    cin>>n>>m>>x>>y;
    memset(map,-1,sizeof map);
    bfs();
}
```

`qhead` 和 `qtail` 用于存放队头和队尾的位置

也可以用 STL queue 代替手搓的对列——

```C++
\#include <iostream>
\#include <cstring>
\#include <queue>
using namespace std;
const int N=410;
int map[N][N],n,m,x,y;
// use [1][1] as point (1,1)
queue<pair<int,int> > q;
void bfs(){
    map[x][y]=0;
    q.push(make_pair(x,y));
    int xmove[8]={2,2,-2,-2,1,-1,1,-1};
    int ymove[8]={1,-1,1,-1,2,2,-2,-2};
    while(!q.empty()){
        pair<int,int> tmp=q.front();q.pop();
        for(int i=0;i<8;i++){
            int x=tmp.first+xmove[i];
            int y=tmp.second+ymove[i];
            if(x<=n&&x>=1&&y<=m&&y>=1&&map[x][y]==-1){
                map[x][y]=map[tmp.first][tmp.second]+1;
                q.push(make_pair(x,y));
            }
        }
    }
    for(int i=1;i<=n;i++){
        for(int j=1;j<=m;j++)
            printf("%-5d",map[i][j]);
        printf("\n");
    }
}
int main() {
    cin>>n>>m>>x>>y;
    memset(map,-1,sizeof map);
    bfs();
}
```

无论使用STL还是手动实现的对列，要记得获取队头数据后将其出队，在手动实现中即为`qhead++`

### BFS 例题 2 填涂颜色

> [!info] 填涂颜色 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1162](https://www.luogu.com.cn/problem/P1162)  

基本思路——

先将可填涂的格子赋值为-1，将不可填涂的格子赋值为-2，将 $i$ 行 `j` 列的点记作`id=i*n+j+1` ，遍历每个点，用其 id 开始填涂，和该点相接的色块全都为该点的 id。

在搜索完成之后`recolor_print()`函数遍历四周，找到和四周相邻的 id 的色块将其剔除出可用范围，最后检测可用且存在于`map`数组中的色块，将其赋值为2，将不可填涂的格子赋值为1，最后输出答案

```C++
\#include <iostream>
\#include <queue>
\#include <cstring>
using namespace std;
int n,map[35][35];
const int xmove[4]={1,-1,0,0};
const int ymove[4]={0,0,1,-1};
void bfs(int X,int Y,int id){
    queue<pair<int,int> > q;
    q.push(make_pair(X,Y));
    map[X][Y]=id;
    while(!q.empty()){
        pair<int,int> tmp=q.front();q.pop();
        for(int i=0;i<4;i++){
            int x=tmp.first+xmove[i];
            int y=tmp.second+ymove[i];
            if(x>=0&&x<n&&y>=0&&y<n&&map[x][y]==-1){
                map[x][y]=id;
                q.push(make_pair(x,y));
            }
        }
    }
}
void initailize(){
    cin>>n;
    for(int i=0;i<n;i++)
        for(int j=0;j<n;j++)
            scanf("%d",&map[i][j]);
    int wall=n*n+1;
    for(int i=0;i<n;i++)
        for(int j=0;j<n;j++)
            map[i][j]=map[i][j]==0?-1:wall;
    // use n*n+1 represent wall
}
void recolor_print(){
    // search the side of the map
    // see if there are some id torch the side
    bool flag[1000];
    // use flag store if the id can be turn into 2
    memset(flag,1,sizeof flag);
    for(int i=0;i<n;i++){
        flag[map[0][i]]=0;
        flag[map[n-1][i]]=0;
        flag[map[i][0]]=0;
        flag[map[i][n-1]]=0;
    }
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            if(map[i][j]==n*n+1)
                map[i][j]=1;
            else if (flag[map[i][j]]==1)
                map[i][j]=2;
            else
                map[i][j]=0;
        }
    }
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++)
            printf("%-2d",map[i][j]);
        printf("\n");
    }
}
int main() {
    initailize();
    for(int i=0;i<n;i++)
        for(int j=0;j<n;j++)
            if(map[i][j]==-1)
                bfs(i,j,i*n+j+1);  
    recolor_print();
}
```

### BFS 例题 3 奇怪的电梯

> [!info] 奇怪的电梯 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1135](https://www.luogu.com.cn/problem/P1135)  

本题是一维 BFS 不需要用 pair 存储点的数据，较为典型的题目

```C++
\#include <iostream>
\#include <queue>
\#include <cstring>
using namespace std;
const int N=205;
int n,a,b,k[N],step[N];
queue<int> q;
int main() {
    memset(step,-1,sizeof step);
    cin>>n>>a>>b;
    for(int i=1;i<=n;i++)
        cin>>k[i];
    step[a]=0;
    q.push(a);
    while(!q.empty()){
        int tmp=q.front();q.pop();
        int depth=step[tmp];
        int up=tmp+k[tmp];
        int dn=tmp-k[tmp];
        if(up>=1&&up<=n&&step[up]==-1){
            q.push(up);
            step[up]=depth+1;
        }
        if(dn>=1&&dn<=n&&step[dn]==-1){
            q.push(dn);
            step[dn]=depth+1;
        }
    }
    cout<<step[b]<<endl;
}
```

# 三、图的存储

树是无环连通图，树是一种特殊的图。图分为有向图和无向图，无向图是一种特殊的有向图。所以只需要知道如何存储有向图就能知道如何存储所有的树和图

### 边列表

一个结构体存放边，三个成员变量 `u,v,w`，分别存起点、终点、边权

### 邻接矩阵

一个边长为`n`的二维数组`arc`，有权重用`int`无权重用`bool`

![[e3ae3bc75be8f54946f39b0c47e16ce2.png]]

### 邻接表 / 链式前向星

`n`个链表，第`i`个单链表存第`i`个点可以到达的所有点，链中的元素之间的顺序无关紧要，一般插入由`x` 到`y` 的边时，让第`x`个链表的表头指向`y` ，然后让`y`再指向原表的第一个元素，简而言之就是一般在链表的前方插入新边

![[bf6a52670ba5baaed416c725870d48cb.png]]

用多个单链表或向量实现的叫做邻接表，用数组和头节点实现的叫做链式前向星，以下模板的是链式前向星

```C++
// 对于每个点k，开一个单链表，存储k所有可以走到的点。h[k]存储这个单链表的头结点
int h[N], e[N], ne[N], idx;

// 添加一条边a->b
void add(int a, int b)
{
    e[idx] = b, ne[idx] = h[a], h[a] = idx ++ ;
}

// 初始化
idx = 0;
memset(h, -1, sizeof h);
```

### 完全二叉树

完全二叉树可以用顺序存储，用一个数组就可以存储，根节点编号为 1

若节点的下标为 `i` 则：

- 左孩子下标为 `2*i`

- 右孩子下标为 `2*i+1`

这种方法只适合完全二叉树使用，否则会占用大量空间

# 四、图的遍历

### 深度优先遍历

一种特殊的 DFS

```C++
int dfs(int u)
{
    st[u] = true; // st[u] 表示点u已经被遍历过

    for (int i = h[u]; i != -1; i = ne[i])
    {
        int j = e[i];
        if (!st[j]) dfs(j);
    }
}
```

### 树的重心

> [!info] 树的重心 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oi-wiki.org/graph/tree-centroid/](https://oi-wiki.org/graph/tree-centroid/)  

如果一个点在树中被拿掉，树被拆成几个部分的最大的那块最小，就说这个点是树的重心

如图拿掉了4就被拆成 5 2 1 个节点的三块（图中这个树的重心是 1 号点，拿掉以后变成 3 4 1 三块，最大值为 4，在所有点的最大值中最小

![[QQ_1730545668664.png]]

求树的重心是一个经典的图的深度优先遍历的应用

参考例题：

[洛谷P1395 会议](https://www.notion.so/P1395-13283793b8f68058bd3acf1913332b6b?pvs=21)  

### 广度优先遍历

一种特殊的 BFS

```C++
queue<int> q;
st[1] = true; // 表示1号点已经被遍历过
q.push(1);

while (q.size())
{
    int t = q.front();
    q.pop();

    for (int i = h[t]; i != -1; i = ne[i])
    {
        int j = e[i];
        if (!st[j])
        {
            st[j] = true; // 表示点j已经被遍历过
            q.push(j);
        }
    }
}
```

### 拓扑排序

> [!info] 拓扑排序 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oi-wiki.org/graph/topo/](https://oi-wiki.org/graph/topo/)  

求拓扑序（Topo）是图的广度优先遍历的经典应用，拓扑序列是一个有向无环图的所有节点排序

如右侧这个图的拓扑序列是 1 2 3。在有向无环图中，所有的边都是由拓扑序列中前方的元素指向后方的元素。有向无环图也被称为拓扑图

在图中，所有元素都有入度和出度，也就是指向的边和被指向的边的数量，如图：

|   |   |   |
|---|---|---|
|节点|入度|出度|
|1|0|2|
|2|1|1|
|3|2|0|

![[QQ_1730546023647.png]]

> **一个有向无环图（DAG, Directed Acyclic Graph）一定存在一个入度为0的点**

在拓扑排序时，任意入度为0的点都可以作为拓扑序的起点，先计算所有点的入度存放在`d[]`中，然后找出所有入度为0的点放入队列`q`中，每次将队头点`x`出队后，迭代找到每个由`x`为起点的边，然后将该边删除，将`x`所指向的那个点的入度减 1，若减 1 后入度为 0，则改点入队

因为有向无环图删除任意一个入度为 0 的点仍然是一个有向无环图，而有向无环图总是有入度为 0 的点，所以上述的迭代操作可以一直进行直到遍历完所有的点

```C++
queue<int> q; // 所有0入度点
vector<int> topo; // 拓扑序答案
while(!q.empty()){
	int tmp=q.front();
	topo.push_back(tmp);
	q.pop();
	while(枚举点tmp所有的出边){
		删除该边
		d[j]--; // 指向的点入度减一
		if(d[j]==0) q.push_back(j);
	}
}
```

> [!info] 【模板】拓扑排序 / 家谱树 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/B3644](https://www.luogu.com.cn/problem/B3644)  

# 五、最短路：朴素 Dijikstra

### 最短路问题——引入

> [!info] 最短路 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oiwiki.org/graph/shortest-path/](https://oiwiki.org/graph/shortest-path/)  

给定一个图、起点、终点，求起点到终点的最短距离就是最短路问题

在最短路径算法中，源点代表起点，汇点代表终点。多元源汇就是求大量多个起点和终点的问题

最短路算法可以分为以下类型（ $n$ 表示点数，$m$ 表示边数）

  

**单源最短路 (Single Source Shortest Path, SSSP)**

- 所有边权都为正
    
    - ==朴素 Dijkstra 算法== ==$O(n^2)$==
    
    - ==堆优化 Dijkstra 算法== ==$O(m\log n)$==
    

- 存在负权边（**NSP**）
    
    - ==Bellman-Ford 算法== ==$O(nm)$==
    
    - ==SPFA 算法== ==$O(m)$==
    

**多源汇最短路 (All-Pairs Shortest Path, APSP)**

- ==Floyd 算法== ==$O(n^3)$==

注：朴素 Dijkstra 时间复杂度和边数无关，适合稠密图，$m$ 的数量级大于 $n$ 时使用。堆优化的 Dijkstra 适合稀疏图，$m$ 和 $n$ 在同一个数量级时使用。SPFA算法是 Bellman-Ford 的优化版本，在最坏情况下会达到 $O(nm)$。Dijisktra 基于贪心，另外三者基于 Dp

### 基本思路

> [!info] 最短路径 迪杰斯特拉 dijkstra算法 数据结构与算法_哔哩哔哩_bilibili  
> 最短路径 迪杰斯特拉 dijkstra算法 数据结构与算法, 视频播放量 31191、弹幕量 84、点赞数 1205、投硬币枚数 328、收藏人数 2091、转发人数 127, 视频作者 图码, 作者简介 更多内容访问【totuma.  
> [https://www.bilibili.com/video/BV1QESyYGE55/](https://www.bilibili.com/video/BV1QESyYGE55/)  

![[QQ_1730733155971.png]]

基本步骤：

- 初始化 dis 数组全为正无限，初始化起点的 dis 为 0

- 循环 n-1 次
    
    - 寻找未确定最短路的点中的 dis 最小的点，记为 t ==$O(n^2)$== ==/== ==$O(n^2)$==
    
    - ==将==点 t 的 final/st 设置为 true ==$O(n)$== ==/== ==$O(n)$==
    
    - 遍历所有以 t 为起点的边，对于每条边，看看边的终点的 dis 是否大于 dis[t] 加该边权，如果是，就更新边的终点的点的 dis ==$O(n^2)$== ==/== ==$O(m)$==
    

- 如果 dis 不是正无限，说明可以从起点出发走到此点，如果是说明无法走到

  

如果需要找到路径，就在每次循环内第三步更新 dis 的时候记录父节点，然后从终点找回到

以上步骤标注的时间复杂度都已经乘上了循环次数 n

使用邻接矩阵的情况下，总的时间复杂度就是 ==$O(n(n+1+n))= O(2n^2+n) \approx O(n^2)$==

使用邻接表的情况下，总的时间复杂度就是 ==$O(n(n+1)+m)= O(n^2+n+m)\approx O(n^2)$==

不难看出朴素 Dijkstra 最慢的一步就是找到未确定最短路 dis 最小的点，这一步就可以用堆/优先队列优化，在下一节会提到

  

### 邻接矩阵模板

```C++
int g[N][N];  // 存储每条边
int dist[N];  // 存储1号点到每个点的最短距离
bool st[N];   // 存储每个点的最短路是否已经确定

// 求1号点到n号点的最短路，如果不存在则返回-1
int dijkstra()
{
    memset(dist, 0x3f, sizeof dist);
    dist[1] = 0;

    for (int i = 0; i < n - 1; i ++ )
    {
        int t = -1;     // 在还未确定最短路的点中，寻找距离最小的点
        for (int j = 1; j <= n; j ++ )
            if (!st[j] && (t == -1 || dist[t] > dist[j]))
                t = j;

        // 用t更新其他点的距离
        for (int j = 1; j <= n; j ++ )
            dist[j] = min(dist[j], dist[t] + g[t][j]);

        st[t] = true;
    }

    if (dist[n] == 0x3f3f3f3f) return -1;
    return dist[n];
}
```

- 在数据小于 $10^9$ 时可以把其初始化为 0x3f3f3f3f，代码可以写成`memset(dist, 0x3f, sizeof dist);`，这样可以防止运算的时候超过数据类型范围

### 邻接表模板

> [!info] 【模板】单源最短路径（弱化版） - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P3371](https://www.luogu.com.cn/problem/P3371)  

```C++
\#include <bits/stdc++.h>
using namespace std;
const int N = 1e4+5; // 最大点数
const int M = 5e5+5; // 最大边数
// 邻接表
int head[N],ver[M]; // 表头 边的终点 
int edge[M],Next[M]; // 边的权值 下一条边的编号
int idx=0;
void add(int u,int v,int w){
  ver[idx]=v;
  edge[idx]=w;
  Next[idx]=head[u];
  head[u]=idx++;
}
// Dijkstra
int dis[N],parent[N]; // 距离数组 父节点
bool vis[N]; // 是否已经是最短路径
int main() {
  // 初始化
  memset(head,-1,sizeof head);
  memset(dis,0x3f,sizeof dis);
  memset(vis,false,sizeof vis);
  // 输入
  int n,m,s; // 点数 边数 起点
  cin>>n>>m>>s;
  for(int i=0;i<m;i++){
    int u,v,w;
    cin>>u>>v>>w;
    add(u,v,w);
  }
  // Dijkstra
  dis[s]=0;
  for(int i=0;i<n-1;i++){ // 循环到n-1即可
    // 1、找到vis为false，距离dis最小的点
    int t=-1;
    for(int j=1;j<=n;j++)
      if(vis[j]==false&&(t==-1 || dis[j]<dis[t]))
        t=j;
    // 2、记录点t已经是最短路
    if(t==-1) break;
    vis[t]=true;
    // 3、用点t更新其他点的距离
    int it=head[t];
    while(it!=-1){
      int nx=ver[it]; // 由点t为起点的边下一个点
      dis[nx]=min(dis[nx],dis[t]+edge[it]);
      it=Next[it];
    }
  }
  // 输出答案
  for(int i=1;i<=n;i++){
    if(dis[i]!=0x3f3f3f3f)
      cout<<dis[i]<<' ';
    else
      cout<<INT_MAX<<' ';
  }
  cout<<endl;
}
```

如果存在重边，就只保留距离最短的，如果存在正自环，则不用管

# 六、最短路：堆优化 Dijkstra

  

### 基本思路

使用堆/优先队列解决循环内的第一步，大大加快了找最小值的速度  

  

- 初始化 dis 数组全为正无限，初始化起点的 dis 为 0

- 循环直到堆空
    
    - 弹出堆顶 dis 最小的点，记为 t，如果已经访问过则略过 ==$O(n)$==
    
    - ==将==点 t 的 final/st 设置为 true ==$O(n)$==
    
    - 遍历所有以 t 为起点的边，对于每条边，看看边的终点的 dis 是否大于 dis[t] 加该边权，如果是，就更新边的终点的点的 dis ==$O(m\log n)$==
    

- 如果 dis 不是正无限，说明可以从起点出发走到此点，如果是说明无法走到

  

以上时间复杂度都乘上了循环次数 n，从小根堆中找最小值的时间复杂度是 $O(1)$ ，每次更新点的 dis 堆需要的时间都是 $O(\log n)$ ，大约每条边都会更新一个点，所以循环内第三步时间复杂度是 $O(m\log n)$ ，总时间复杂度为 ==$O(n+n+m\log n)\approx O(m\log n)$==

### 邻接表+优先队列模板

```C++
typedef pair<int, int> PII;

int n;      // 点的数量
int h[N], w[N], e[N], ne[N], idx;       // 邻接表存储所有边
int dist[N];        // 存储所有点到1号点的距离
bool st[N];     // 存储每个点的最短距离是否已确定

// 求1号点到n号点的最短距离，如果不存在，则返回-1
int dijkstra()
{
    memset(dist, 0x3f, sizeof dist);
    dist[1] = 0;
    priority_queue<PII, vector<PII>, greater<PII>> heap;
    heap.push({0, 1});      // first存储距离，second存储节点编号

    while (heap.size())
    {
        auto t = heap.top();
        heap.pop();

        int ver = t.second, distance = t.first;

        if (st[ver]) continue;
        st[ver] = true;

        for (int i = h[ver]; i != -1; i = ne[i])
        {
            int j = e[i];
            if (dist[j] > distance + w[i])
            {
                dist[j] = distance + w[i];
                heap.push({dist[j], j});
            }
        }
    }

    if (dist[n] == 0x3f3f3f3f) return -1;
    return dist[n];
}
```

注意，无论是在朴素版还是堆优化版的 Dijkstra 中，判断点是否访问过都是在循环内第一步进行的，所以在堆优化版中要在取得堆顶后立即判断点是否已经找到了最短路

在堆优化的外层循环中，使用`while(!q.empty())` 作为循环条件，这个队列其实并不是 dis 数组的代替品，而是一个额外的辅助数据结构，它用于找到最小的 dis，所以同一个点会被加入堆中多次，如果遇到了已经找到最短路的点就直接 `continue`

### 模板题

> [!info] 【模板】单源最短路径（标准版） - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P4779](https://www.luogu.com.cn/problem/P4779)  

```C++
\#include <bits/stdc++.h>
using namespace std;
const int N = 1e5+10;
const int M = 5e5+10;
\#define PII pair<int,int>
\#define fi first
\#define se second
int n; // 点数
// 邻接表
int head[N],ver[M]; // 头节点 边终点
int edge[M],Next[M],idx=0; // 边权 指针
void add(int u,int v,int w){
  ver[idx]=v;
  edge[idx]=w;
  Next[idx]=head[u];
  head[u]=idx++;
}
// dijkstra
void dijstra(int start){
  bool vis[N];
  int dis[N];
  memset(vis,0,sizeof vis);
  memset(dis,0x3f,sizeof dis);
  dis[start]=0;
  // first存储距离，second存储节点编号
  priority_queue<PII,vector<PII>,greater<PII> > q;
  q.push(make_pair(0,start));
  while(!q.empty()){
    // 取出堆顶，即当前距离最小的点
    int tdis=q.top().fi;
    int t=q.top().se;
    q.pop();
    // 判断该点是否已经找到了最短路
    if(vis[t]==true) continue;
    // 将点t标记为已访问
    vis[t]=true;
    // 遍历t的所有边
    int it=head[t];
    while(it!=-1){
      int nx=ver[it];
      int nedge=edge[it];
      if(tdis+nedge<dis[nx]){
        dis[nx]=tdis+nedge;
        q.push(make_pair(dis[nx],nx));
      }
      it=Next[it];
    }
  }
  for(int i=1;i<=n;i++){
    if(dis[i]!=0x3f3f3f3f)
      cout<<dis[i]<<' ';
    else 
      cout<<INT_MAX<<' ';
  }
  cout<<endl;
  return;
}
int main() {
  // 初始化
  ios::sync_with_stdio(false);
  memset(head,-1,sizeof head);
  // 输入
  int m,s; // 边数 起点
  cin>>n>>m>>s;
  for(int i=0;i<m;i++){
    int u,v,w;
    cin>>u>>v>>w;
    add(u,v,w);
  }
  dijstra(s);
}
```

  

# 七、最短路：Bellman-Ford

### 基本思路

Bellman-Ford 算法专注于解决有负权边的 SSSP 问题，有 n 个点和 m 条边，使用边列表储存图，算法步骤如下：

  

- 初始化所有的 dis 为无限大，设置起点 dis 为 0

- 循环 n 次
    
    - 每次遍历所有的边
        
        - 更新最小距离（松弛操作）
        
    

三角不等式：循环 n 次后 dis[b] 一定小于任意 dis[a]+w

然而，NSP 问题棘手的地方在于负权回路，如果负权回路被一直迭代，那么回路上所有点 dis 就会变成负无限，从而导致整个图都更新成负无限

如果第n次迭代仍然会松弛三角不等式，就说明存在一条长度是 n+1 的最短路径，由抽屉原理，路径中至少存在两个相同的点，说明图中存在负权回路

所以不考虑负环的 Bellman-Ford 算法只需要循环 n-1 次，也可以利用第 n 次循环判断是否进行更新了 dis 以判断是否有负环

### 最多经过 k 条边

还可以利用限制循环次数来解 “最多经过 k 条边的最短路” 的题，但是要备份 dis 数组，保证更新时只会使用上一次迭代完所有边的结果

串联的影响如图，在第一次遍历所有边时就更新了 3 号点的距离，所以要先用`memcpy()`备份一份 dis 数组，每次更新使用备份数组判断，修改进 dis 数组。

备份操作需要在循环内第一步进行，也就是遍历所有边之前

`dis[v]=min(dis[v],backup[u]+w);`

![[QQ_1730869853528.png]]

```C++
int n, m;       // n表示点数，m表示边数
int dist[N];        // dist[x]存储1到x的最短路距离

struct Edge     // 边，a表示出点，b表示入点，w表示边的权重
{
    int a, b, w;
}edges[M];

// 求1到n的最短路距离，如果无法从1走到n，则返回-1。
int bellman_ford()
{
    memset(dist, 0x3f, sizeof dist);
    dist[1] = 0;

    // 如果第n次迭代仍然会松弛三角不等式，就说明存在一条长度是n+1的最短路径，由抽屉原理，路径中至少存在两个相同的点，说明图中存在负权回路。
    for (int i = 0; i < n; i ++ )
    {
        for (int j = 0; j < m; j ++ )
        {
            int a = edges[j].a, b = edges[j].b, w = edges[j].w;
            if (dist[b] > dist[a] + w)
                dist[b] = dist[a] + w;
        }
    }

    if (dist[n] > 0x3f3f3f3f / 2) return -1;
    return dist[n];
}
```

### 模板题

> [!info] 租用游艇 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1359](https://www.luogu.com.cn/problem/P1359)  

```C++
\#include <bits/stdc++.h>
using namespace std;
const int N = 205;
struct edge{
  int u,v,w;
}edges[N*N];
int n,idx=0;
int main() {
  // input
  cin>>n;
  for(int i=1;i<n;i++){
    for(int j=i+1;j<=n;j++){
      cin>>edges[idx].w;
      edges[idx].u=i,edges[idx].v=j;
      idx++;
    }
  }
  // Bellman-Ford
  int dis[N];
  memset(dis,0x3f,sizeof(dis));
  dis[1]=0;
  for(int i=0;i<n-1;i++){
    for(int j=0;j<idx;j++){
      edge a=edges[j];
      dis[a.v]=min(dis[a.v],dis[a.u]+a.w);
    }
  }
  cout<<dis[n]<<endl;
}
```

### 模板题：负环

> [!info] 【模板】负环 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P3385](https://www.luogu.com.cn/problem/P3385)  

```C++
\#include <bits/stdc++.h>
using namespace std;
const int N = 2e3+5;
const int M = 3e3+5;
struct edge{
  int u,v,w;
}e[N*N];
void f(){
  int n,m;
  cin>>n>>m;
  int idx=0;
  for(int i=0;i<m;i++){
    edge a;
    cin>>a.u>>a.v>>a.w;
    e[idx++]=a;
    if(a.w>=0){
      swap(a.u,a.v);
      e[idx++]=a;
    }
  }
  // bellman-ford
  int dis[N];
  memset(dis,0x3f,sizeof(dis));
  dis[1]=0;
  for(int i=0;i<n-1;i++){
    for(int j=0;j<idx;j++){
      edge a=e[j];
      // 此步判断可有可无，可以减少时间复杂度
      if(dis[a.u]==0x3f3f3f3f) continue;
      // relax
      dis[a.v]=min(dis[a.v],dis[a.u]+a.w);
    }
  }
  // check negative cycle
  for(int j=0;j<idx;j++){
    edge a=e[j];
    // 缺少了 dis[a.u]!=0x3f3f3f3f 判断会导致一个 WA
    if(dis[a.v]>dis[a.u]+a.w && dis[a.u]!=0x3f3f3f3f){
      cout<<"YES\n";
      return;
    }
  }
  cout<<"NO\n";
  return;
}
int main() {
  int t;
  cin>>t;
  while(t--) f(); 
}
```

这道题要注意判断负环时点是否是不能到达的，如果能到达而且更新了才算是有负环

# 八、最短路：SPFA

### 基本思路

SPFA 就是 Bellman-Ford 算法的优化。考虑到每次更新最小距离的时候，只有前一个点被更新过了才有可能去更新下一个点，基本思路和 BFS 类似，每次把 dis 更新过（变小过）的节点放入一个队列，然后去更新以队头点为起点的所有边的终点，直到队列为空（没有点可以变小了）

  

- 初始化 dis 数组全为正无限，初始化起点的 dis 为 0

- 向队列中入队起点

- while 队列不为空
    
    - 将队头点 t 取出，对头出队
    
    - 更新 t 的所有出边，如果有点被更新就入队
    

为了防止向队列中加入重复的点，需要用 bool 数组进行记录

```C++
int n;      // 总点数
int h[N], w[N], e[N], ne[N], idx;       // 邻接表存储所有边
int dist[N];        // 存储每个点到1号点的最短距离
bool st[N];     // 存储每个点是否在队列中

// 求1号点到n号点的最短路距离，如果从1号点无法走到n号点则返回-1
int spfa()
{
    memset(dist, 0x3f, sizeof dist);
    dist[1] = 0;

    queue<int> q;
    q.push(1);
    st[1] = true;

    while (q.size())
    {
        auto t = q.front();
        q.pop();

        st[t] = false;

        for (int i = h[t]; i != -1; i = ne[i])
        {
            int j = e[i];
            if (dist[j] > dist[t] + w[i])
            {
                dist[j] = dist[t] + w[i];
                if (!st[j])     // 如果队列中已存在j，则不需要将j重复插入
                {
                    q.push(j);
                    st[j] = true;
                }
            }
        }
    }

    if (dist[n] == 0x3f3f3f3f) return -1;
    return dist[n];
}
```

### 例题

标准版只有堆优化的 Dijkstra 能过

> [!info] 【模板】单源最短路径（弱化版） - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P3371](https://www.luogu.com.cn/problem/P3371)  

```C++
\#include <bits/stdc++.h>
using namespace std;
const int N = 1e5+5;
const int M = 5e5+5;
int head[N],ver[M],edge[M],Next[M],idx=0;
void add(int u,int v,int w){
  ver[idx]=v;
  edge[idx]=w;
  Next[idx]=head[u];
  head[u]=idx++;
}
int main() {
  memset(head,-1,sizeof(head));
  int n,m,s; // vertices, edges, source
  cin>>n>>m>>s;
  int u,v,w;
  for(int i=0;i<m;i++){
    cin>>u>>v>>w;
    add(u,v,w);
  }
  // SPFA
  int dis[N];
  bool vis[N];
  queue<int> q;
  memset(dis,0x3f,sizeof(dis));
  memset(vis,0,sizeof(vis));
  dis[s]=0;
  vis[s]=1;
  q.push(s);
  while(!q.empty()){
    int t=q.front();
    q.pop();
    vis[t]=0;
    int it=head[t];
    while(it!=-1){
      int v=ver[it];
      int w=edge[it];
      if(dis[v]>dis[t]+w){
        dis[v]=dis[t]+w;
        if(!vis[v]){
          q.push(v);
          vis[v]=1;
        }

      }
      it=Next[it];
    }
  }
  for(int i=1;i<=n;i++){
    if(dis[i]!=0x3f3f3f3f)
      cout<<dis[i]<<' ';
    else
      cout<<INT_MAX<<' ';
  }
  cout<<endl;
}
```

### 负环问题

SPFA 可以维护一个 cnt[N] 数组，用于记录从起点到该点的最短路径经过的边数，如果在遍历边的时候发现某点的 cnt 大于等于 n 则说明存在负环，因为不存在负环的情况下每个点不可能走两次，cnt 最多是 n-1，此时立即退出外层while 循环。

在用 SPFA找负环的题中，因为没有固定起点，有可能负环和一开始放入队列的一号点并不联通，所以要将所有点都先放入队列中

```C++
int n;      // 总点数
int h[N], w[N], e[N], ne[N], idx;       // 邻接表存储所有边
int dist[N], cnt[N];        // dist[x]存储1号点到x的最短距离，cnt[x]存储1到x的最短路中经过的点数
bool st[N];     // 存储每个点是否在队列中

// 如果存在负环，则返回true，否则返回false。
bool spfa()
{
    // 不需要初始化dist数组
    // 原理：如果某条最短路径上有n个点（除了自己），那么加上自己之后一共有n+1个点，由抽屉原理一定有两个点相同，所以存在环。

    queue<int> q;
    for (int i = 1; i <= n; i ++ )
    {
        q.push(i);
        st[i] = true;
    }

    while (q.size())
    {
        auto t = q.front();
        q.pop();

        st[t] = false;

        for (int i = h[t]; i != -1; i = ne[i])
        {
            int j = e[i];
            if (dist[j] > dist[t] + w[i])
            {
                dist[j] = dist[t] + w[i];
                cnt[j] = cnt[t] + 1;
                // 如果从1号点到x的最短路中包含至少n个点（不包括自己），则说明存在环
                if (cnt[j] >= n) return true;
                if (!st[j])
                {
                    q.push(j);
                    st[j] = true;
                }
            }
        }
    }

    return false;
}
```

# 九、最短路：**Floyd-Warshall**

  

### 思路和原理

> [!info] 【五分钟速通】巨快无比秒杀Floyd算法手动模拟流程_哔哩哔哩_bilibili  
> 极速版Floyd！, 视频播放量 42335、弹幕量 62、点赞数 1175、投硬币枚数 674、收藏人数 2074、转发人数 298, 视频作者 Prism菌, 作者简介 如果你看到她 回到海岸，相关视频：求最短路径Floyd算法！，又快又准做对考研真题，从考试的角度出发【弗洛伊德算法】【floyed】【多源最短路径】，【五分钟速通】Dijkstra算法手动模拟流程，「六分钟速通」KMP算法求解流程，最短路径（三）Floyd算法，Floyd算法讲解，最短路径Floyd算法，[Day51] 最短路径问题，BFS算法，Dijkstra算法，Floyd算法，15分钟速通弗洛伊德(Floyd)算法【证明 + Rust实现】，边睡边学算法丨第一期  
> [https://www.bilibili.com/video/BV1tQ4y147pw](https://www.bilibili.com/video/BV1tQ4y147pw)  

Floyd-Warshall 算法的代码实现较为简单，参考下面的代码模板，基本步骤可以抽象理解为：

输入和初始化，若有边 $i\rightarrow j$ 且权重为 $w$，则 $d(i,j)=w$ ，若 $i=j$ 则 $d(i,j)=0$，否则 $d(i,j)=+\infty$

k 代表下图中的红色竖线位置，`d[i][k]`、`d[k][j]`都是红色竖线上的点，然后遍历每一个点，找到这个点在红色竖线上的垂足点，用两个垂足点的和更新距离

![[QQ_1730874049963.png]]

Floyd 算法原理基于动态规划，三维 $d(k,i,j)$ 表示仅能经过 $1～k$ 号点的情况下，由点 $i$ 走到 $j$ 的最短距离

$$d(k,i,j)=min(d(k−1,i,j),d(k−1,i,k)+d(k−1,k,j))$$

优化至二维去掉了 $k$，就是 Floyd 算法

$$d(i,j)=min(d(i,j),d(i,k)+d(k,j))$$

```C++
//初始化：
    for (int i = 1; i <= n; i ++ )
        for (int j = 1; j <= n; j ++ )
            if (i == j) d[i][j] = 0;
            else d[i][j] = INF;

// 算法结束后，d[a][b]表示a到b的最短距离
void floyd()
{
    for (int k = 1; k <= n; k ++ )
        for (int i = 1; i <= n; i ++ )
            for (int j = 1; j <= n; j ++ )
                d[i][j] = min(d[i][j], d[i][k] + d[k][j]);
}
```

### 例题

> [!info] 邮递员送信 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/P1629](https://www.luogu.com.cn/problem/P1629)  

```C++
\#include <bits/stdc++.h>
using namespace std;
const int N = 1e3+10;
const int M = 1e5+10;
int main() {
  int n,m,d[N][N];
  memset(d,0x3f,sizeof d);
  cin>>n>>m;
  int u,v,w;
  for(int i=0;i<m;i++){
    cin>>u>>v>>w;
    d[u][v]=min(d[u][v],w);
  }
  for(int i=1;i<=n;i++) d[i][i]=0;
  for(int k=1;k<=n;k++)
    for(int i=1;i<=n;i++)
      for(int j=1;j<=n;j++)
        d[i][j]=min(d[i][j],d[i][k]+d[k][j]);
  int ans=0;
  for(int i=1;i<=n;i++)
    ans+=d[1][i]+d[i][1];
  cout<<ans<<endl;
}
```

由于道路不是双向的，所以从 1 到 $i$ 点和从 $i$ 到 1 点路径长度不一样，用 Floyd 做较为方便

Floyd 求闭包：

> [!info] 【模板】传递闭包 - 洛谷  
>  
> [https://www.luogu.com.cn/problem/B3611](https://www.luogu.com.cn/problem/B3611)  

# 十、最小生成树：Prim

### 最小生成树问题——引入

给定一个 n 个点的图，求一棵树联通所有点且这颗树的边权和最小，该树将拥有 n-1 条边，而且一定无环，转换成有向边后一定联通。这就是最小生成树问题，在大部分情况下，最小生成树问题都是针对无向图的

> [!info] 最小生成树 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oiwiki.org/graph/mst/](https://oiwiki.org/graph/mst/)  

最小生成树算法分类

- 普利姆算法 Prim 算法，类似 Dijkstra
    
    - 朴素版：稠密图 $O(n^2)$
    
    - 堆优化版：稀疏图 $O(m\log n)$
    

- 克鲁斯卡尔 Kruskal 算法 $O(m\log m)$
    
    - 稀疏图常用，一般不用堆优化 Prim
    

### 基本思路

> [!info] 图-最小生成树-Prim(普里姆)算法和Kruskal(克鲁斯卡尔)算法_哔哩哔哩_bilibili  
> 原创(禁止搬运, 二改), 欢迎大家点赞收藏+关注, 本人长期更新考研计算机相关内容, 关注不跟丢。, 视频播放量 53123、弹幕量 329、点赞数 2044、投硬币枚数 1429、收藏人数 1614、转发人数 372, 视频作者 蓝不过海呀, 作者简介 计算机知识分享&讲解，相关视频：最小生成树(Kruskal(克鲁斯卡尔)和Prim(普里姆))算法动画演示，图-最短路径-Dijkstra(迪杰斯特拉)算法，边睡边学算法丨第一期，克鲁斯卡尔算法代码可视化(Kruskal)，不能说毫不相干，简直是一模一样(Prim vs Dijkstra)，最小生成树的概念和算法详解，普里姆和迪杰斯特拉太像了，他们有什么区别？，最小生成树—prim算法，数据结构-图-prim（普里姆）算法最小生成树（过程分析+手写代码），数据结构 kruskal克鲁斯卡尔算法求最小生成树权值（并查集/压缩路径/代码）  
> [https://www.bilibili.com/video/BV1wG411z79G/](https://www.bilibili.com/video/BV1wG411z79G/)  

Prim 的基本思路和 Dijkstra 类似，Dijkstra 是每次找到 dis 最小的没有确定最短距的点，然后遍历其所有出边。Prim 是每次找到离树最近的没有加入到树的的点加入树，也就是找整棵树最小的出边，然后把出边对应的点加入到树中

不过在实际实现中，并不会每次加点都遍历所有的边然后加入点，而是像 Dijkstra 一样维护一个 dis 数组，直接找到 dis 的最小值即可，所以 Prim 算法以找点为核心，时间复杂度和边无关，更适合稠密图

  

- 初始化 dis 为正无限

- 循环 n 次（加点 n 次）
    
    - 找到离集合最近的点 t（ dis最小 ）
    
    - 标记点 t 已经在树中
    
    - 用 t 更新其他点的距离
    

以下代码不像 Djikstra 先选择一个点，所以要循环 n 次而不是 n-1 次。由于要找最小边权和，所以还要维护 res，**而且还要特别注意先累加 res 在去执行第三步的更新，防止有负自环更新了 dis[t] 本身**

**还特别要注意的是，更新距离的时候，更新的不是该点到起点的位置，而是到树的位置**，所以是用边权和 dis 直接做比较：`min(dist[j], g[t][j]);` 而不是像 Dijkstra 一样

```C++
int n;      // n表示点数
int g[N][N];        // 邻接矩阵，存储所有边
int dist[N];        // 存储其他点到当前最小生成树的距离
bool st[N];     // 存储每个点是否已经在生成树中


// 如果图不连通，则返回INF(值是0x3f3f3f3f), 否则返回最小生成树的树边权重之和
int prim()
{
    memset(dist, 0x3f, sizeof dist);

    int res = 0;
    for (int i = 0; i < n; i ++ )
    {
        int t = -1;
        for (int j = 1; j <= n; j ++ )
            if (!st[j] && (t == -1 || dist[t] > dist[j]))
                t = j;

        if (i && dist[t] == INF) return INF;

        if (i) res += dist[t];
        st[t] = true;

        for (int j = 1; j <= n; j ++ ) dist[j] = min(dist[j], g[t][j]);
    }

    return res;
}
```

# 十一、最小生成树：Kruskal

### 基本思路

克鲁斯卡尔算法以加边为核心，每次找到最小的边，判断边的两端在不在一个连通分量里，如果不在就连通，其核心是并查集

  

- 按权排序所有边 $O(m\log m)$

- 枚举每条边 $O(m)$
    
    - 对于边 $a\rightarrow b$，如果点 a b 不连通
        
        - 就将边加入树中
        
    

```C++
int n, m;       // n是点数，m是边数
int p[N];       // 并查集的父节点数组

struct Edge     // 存储边
{
    int a, b, w;

    bool operator< (const Edge &W)const
    {
        return w < W.w;
    }
}edges[M];

int find(int x)     // 并查集核心操作
{
    if (p[x] != x) p[x] = find(p[x]);
    return p[x];
}

int kruskal()
{
    sort(edges, edges + m);

    for (int i = 1; i <= n; i ++ ) p[i] = i;    // 初始化并查集

    int res = 0, cnt = 0;
    for (int i = 0; i < m; i ++ )
    {
        int a = edges[i].a, b = edges[i].b, w = edges[i].w;

        a = find(a), b = find(b);
        if (a != b)     // 如果两个连通块不连通，则将这两个连通块合并
        {
            p[a] = b;
            res += w;
            cnt ++ ;
        }
    }

    if (cnt < n - 1) return INF;
    return res;
}
```

# 十二、二分图：染色法

### 二分图问题——引入

> 二分图，又称二部图，英文名叫 Bipartite graph。
> 
> 二分图是什么？节点由两个集合组成，且两个集合内部没有边的图。
> 
> 换言之，存在一种方案，将节点划分成满足以上性质的两个集合。

二分图的另一个定义就是：不含奇数环的图，奇数环就是包含奇数个点的环

> [!info] 二分图 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oiwiki.org/graph/bi-graph/](https://oiwiki.org/graph/bi-graph/)  

二分图算法分类

- 染色法 $O(n+m)$

- 匈牙利算法 $O(nm)$
    
    - 实际运训时间远小于 $O(nm)$
    

### 基本思路

染色法的作用就是通过判断是否不含奇数环判断一个图是不是二分图

随便找到一个点将其染成白色，然后深搜或广搜找点，每次将未染色的点染成与上一个点相反的颜色，这个过程一定是不会将白色染成黑色或者将黑色染成白色的，因为如果出现染相反色的情况，说明存在奇数环

有 n 个点，伪代码如下：

- flag 初始化为 true

- 循环 n 次
    
    - 如果点 i 未染色
        
        - `dfs(i, 1);`且如果返回了 false，则将 flag 标记为 false，退出循环
        
    

- 如果 flag 为 true，则二分图成功，否则失败

- dfs（将点 u 染色为颜色 c，返回false代表有冲突）
    
    - 将点 u 染色为 c
    
    - 遍历点 u 所有出边，边终点为 j
        
        - 如果点 j 没有染色
            
            - 染色，如果染色冲突则返回 false
            
        
        - 如果点 j 染过色
            
            - 如果颜色和当前点相同则返回 false
            
        
    
    - 返回 true
    

```C++
int n;      // n表示点数
int h[N], e[M], ne[M], idx;     // 邻接表存储图
int color[N];       // 表示每个点的颜色，-1表示未染色，0表示白色，1表示黑色

// 参数：u表示当前节点，c表示当前点的颜色
bool dfs(int u, int c)
{
    color[u] = c;
    for (int i = h[u]; i != -1; i = ne[i])
    {
        int j = e[i];
        if (color[j] == -1)
        {
            if (!dfs(j, !c)) return false;
        }
        else if (color[j] == c) return false;
    }

    return true;
}

bool check()
{
    memset(color, -1, sizeof color);
    bool flag = true;
    for (int i = 1; i <= n; i ++ )
        if (color[i] == -1)
            if (!dfs(i, 0))
            {
                flag = false;
                break;
            }
    return flag;
}
```

# 十三、二分图：匈牙利算法

匈牙利算法可以找到二分图左右两侧最大的匹配数量，也就是说可以找到最大数量的边，使得所有的边都没有共用任何点

### 基本思路

遍历左边的点，尝试匹配右边的点，如果右边的点已经被匹配，就递归尝试将匹配右边的左边点换一个点匹配

> [!info] 二分图最大权匹配 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oiwiki.org/graph/graph-matching/bigraph-weight-match/](https://oiwiki.org/graph/graph-matching/bigraph-weight-match/)  

```C++
int n1, n2;     // n1表示第一个集合中的点数，n2表示第二个集合中的点数
int h[N], e[M], ne[M], idx;     // 邻接表存储所有边，匈牙利算法中只会用到从第一个集合指向第二个集合的边，所以这里只用存一个方向的边
int match[N];       // 存储第二个集合中的每个点当前匹配的第一个集合中的点是哪个
bool st[N];     // 表示第二个集合中的每个点是否已经被遍历过

bool find(int x)
{
    for (int i = h[x]; i != -1; i = ne[i])
    {
        int j = e[i];
        if (!st[j])
        {
            st[j] = true;
            if (match[j] == 0 || find(match[j]))
            {
                match[j] = x;
                return true;
            }
        }
    }

    return false;
}

// 求最大匹配数，依次枚举第一个集合中的每个点能否匹配第二个集合中的点
int res = 0;
for (int i = 1; i <= n1; i ++ )
{
    memset(st, false, sizeof st);
    if (find(i)) res ++ ;
}
```