---
标签:
  - 杂项
难度:
  - ★
---
> [!info] 复杂度简介 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oi-wiki.org/basic/complexity/](https://oi-wiki.org/basic/complexity/)  

# 时间复杂度

## 由数据范围反推算法复杂度以及算法内容

一般 ACM 或者笔试题的时间限制是1秒或2秒。  
在这种情况下，C++ 代码中的操作次数控制在 $10^7～10^8$ 为最佳。

下面给出在不同数据范围下，代码的时间复杂度和算法该如何选择：

$n≤30$, 指数级别, dfs+剪枝，状态压缩dp$n≤100$ => $O(n^3)$，floyd，dp，高斯消元$n≤1000$ => $O(n^2)$，$O(n^2\log n)$，dp，二分，朴素版Dijkstra、朴素版Prim、Bellman-Ford$n≤10000$ => $O(n\sqrt n)$，块状链表、分块、莫队$n≤100000$ => $O(n\log n)$ => 各种sort，线段树、树状数组、set/map、heap、拓扑排序、dijkstra+heap、prim+heap、Kruskal、spfa、求凸包、求半平面交、二分、CDQ分治、整体二分、后缀数组、树链剖分、动态树$n≤1000000$ => $O(n)$, 以及常数较小的 $O(n\log n)$ 算法 => 单调队列、 hash、双指针扫描、BFS、并查集，kmp、AC自动机，常数比较小的 $O(n\log n)$ 的做法：sort、树状数组、heap、dijkstra、spfa$n≤10000000$ => $O(n)$，双指针扫描、kmp、AC自动机、线性筛素数$n≤10^9$ => $O(\sqrt n)$，判断质数$n≤10^{18}$ => $O(\log n)$，最大公约数，快速幂，数位DP$n≤10^{1000}$ => $O((\log n)^2)$，高精度加减乘除$n≤10^{100000}$=> $O(logk×loglogk)$，k表示位数，高精度加减、FFT/NTT

作者：yxc  
链接：[https://www.acwing.com/file_system/file/content/whole/index/content/3074/](https://www.acwing.com/file_system/file/content/whole/index/content/3074/)  
来源：AcWing

## 由代码推时间复杂度

### 循环

比较简单，看层数看循环次数，有一种特殊的情况，如果某一层循环只加不减，如 KMP 的内层循环，那么这一层不计入时间复杂计算，所以 KMP 为 $O(n)$

### 递归

> [!info] 复杂度简介 - OI Wiki  
> OI Wiki 是一个编程竞赛知识整合站点，提供有趣又实用的编程竞赛知识以及其他有帮助的内容，帮助广大编程竞赛爱好者更快更深入地学习编程竞赛  
> [https://oi-wiki.org/basic/complexity/#主定理-master-theorem](https://oi-wiki.org/basic/complexity/#主定理-master-theorem)  

> [!info] 深入浅出理解主定理原理(Master theorem)如何计算递归时间复杂度 | RESTKHZ  
> 深入浅出介绍主定理的原理(Master theorem)以及我们到底应该如何使用  
> [https://blog.restkhz.com/post/how-master-theorem-works](https://blog.restkhz.com/post/how-master-theorem-works)  

  

## 其他技巧

$\log_2 10^x=x\log_2 10\approx3x$，所以 long long $10^{18}$ 范围内时间 log 复杂度差不多就是 18x3=64

动态规划计算：状态数量$\times$状态转移计算量

  

# 空间复杂度

## 单位换算

1 Byte = 8 bit

1 KB = 1024 Byte = $2^{10}$ Byte

1 MB = 1024$\times$1024 Byte = $2^{20}$ Byte

## C++数据类型占用空间

char 1 Byte，int 4 Byte，double 8 Byte，long long 8 Byte

指针 8 Byte （64位系统）4 Byte （32位系统）

bool 1 Byte，而不是 1 bit，bitset 是 1bit

## 其他技巧

- 64 MB = $2^{26}$ Byte 可以存下 $2^{24}$ 个 int，约等于 $1.6e7$ 也就是 1600 万个 int

- 程序的其他地方也需要内存，不能顶着内存上限开数组

- 如果有数组 `int a[N][N],b[N][N],c[N]` 可以用 `cout << (sizeof a + sizeof b + sizeof c) / 1024 / 1024 << endl;` 查看内存占用，单位是MB

- 递归本身也需要空间，由层数决定，比如说没有开任何额外数组的快速排序，由于递归了 $\log n$层，所以需要额外的 $O(\log n)$ 空间