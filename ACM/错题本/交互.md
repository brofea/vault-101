# 打A错题本：博弈

## [CF 2001 C](https://codeforces.com/problemset/problem/2001/C) 【AC】

有一个 $n$ 节点的树，每次询问 `? a b` 会返回和 a b 距离之差的绝对值最小的点，在 $15n$ 次询问内找出树的所有边

尝试链接 1 和 i(2-n)，每次询问 `? l r` 得到 `d` 若是 `l r` 之一，则说明 `l r` 之间有边，否则二分递归询问 `? l d` 和 `? r d`。维护一个 DSU，每找到一条边就合并，若某次询问到两个已经连通点就略过，以此剪枝