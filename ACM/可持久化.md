---
标签:
  - 数据结构
难度:
  - ★★★
---
# 引入

可持久化的数据结构类似 Git，可以保存所有的历史版本，可细分为两种：

- 部分可持久化：所有版本可访问，最新版本可修改

- 完全可持久化：所有版本可访问、可修改

- 合流式持久化：所有版本可访问、可修改，且支持两个版本合并

可持久化的核心思想就是只记录每个版本与前一个版本不同的节点，每次需要修改的节点为 $\log n$ 个，时间复杂度为 $O(m\log n)$，$m$ 是修改总数，$n$ 是节点个数

# 可持久化 Trie