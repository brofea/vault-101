# 最小生成树

给定一个 n 个点的图，求一棵树联通所有点且这颗树的边权和最小，该树将拥有 n-1 条边，而且一定无环，转换成有向边后一定联通。这就是最小生成树问题，在大部分情况下，最小生成树问题都是针对无向图的

> [OI Wiki - 最小生成树](https://oiwiki.org/graph/mst/)  

最小生成树算法分类

- 普利姆算法 Prim 算法，类似 Dijkstra
  - 朴素版：稠密图 $O(n^2)$
  - 堆优化版：稀疏图 $O(m\log n)$
- 克鲁斯卡尔 Kruskal 算法 $O(m\log m)$
  - 稀疏图常用，一般不用堆优化 Prim

## Prim

> [哔哩哔哩 - 图-最小生成树-Prim(普里姆)算法和Kruskal(克鲁斯卡尔)算法](https://www.bilibili.com/video/BV1wG411z79G/)

Prim 的基本思路和 Dijkstra 类似，Dijkstra 是每次找到 dis 最小的没有确定最短距的点，然后遍历其所有出边。Prim 是每次找到离树最近的没有加入到树的的点加入树，也就是找整棵树最小的出边，然后把出边对应的点加入到树中

不过在实际实现中，并不会每次加点都遍历所有的边然后加入点，而是像 Dijkstra 一样维护一个 dis 数组，直接找到 dis 的最小值即可，所以 Prim 算法以找点为核心，时间复杂度和边无关，更适合稠密图

- 初始化 dis 为正无限
- 循环 n 次（加点 n 次）
  - 找到离集合最近的点 t（ dis最小 ）
  - 标记点 t 已经在树中
  - 用 t 更新其他点的距离

以下代码不像 Djikstra 先选择一个点，所以要循环 n 次而不是 n-1 次。由于要找最小边权和，所以还要维护 res，**而且还要特别注意先累加 res 在去执行第三步的更新，防止有负自环更新了 dis[t] 本身**

**还特别要注意的是，更新距离的时候，更新的不是该点到起点的位置，而是到树的位置**，所以是用边权和 dis 直接做比较：`min(dist[j], g[t][j]);` 而不是像 Dijkstra 一样

```C++
int n;      // n表示点数
int g[N][N];        // 邻接矩阵，存储所有边
int dist[N];        // 存储其他点到当前最小生成树的距离
bool st[N];     // 存储每个点是否已经在生成树中


// 如果图不连通，则返回INF(值是0x3f3f3f3f), 否则返回最小生成树的树边权重之和
int prim()
{
    memset(dist, 0x3f, sizeof dist);

    int res = 0;
    for (int i = 0; i < n; i ++ )
    {
        int t = -1;
        for (int j = 1; j <= n; j ++ )
            if (!st[j] && (t == -1 || dist[t] > dist[j]))
                t = j;

        if (i && dist[t] == INF) return INF;

        if (i) res += dist[t];
        st[t] = true;

        for (int j = 1; j <= n; j ++ ) dist[j] = min(dist[j], g[t][j]);
    }

    return res;
}
```

## Kruskal

克鲁斯卡尔算法以加边为核心，每次找到最小的边，判断边的两端在不在一个连通分量里，如果不在就连通，其核心是并查集

- 按权排序所有边 $O(m\log m)$
- 枚举每条边 $O(m)$
  - 对于边 $a\rightarrow b$，如果点 a b 不连通
    - 就将边加入树中

```C++
int n, m;       // n是点数，m是边数
int p[N];       // 并查集的父节点数组

struct Edge     // 存储边
{
    int a, b, w;

    bool operator< (const Edge &W)const
    {
        return w < W.w;
    }
}edges[M];

int find(int x)     // 并查集核心操作
{
    if (p[x] != x) p[x] = find(p[x]);
    return p[x];
}

int kruskal()
{
    sort(edges, edges + m);

    for (int i = 1; i <= n; i ++ ) p[i] = i;    // 初始化并查集

    int res = 0, cnt = 0;
    for (int i = 0; i < m; i ++ )
    {
        int a = edges[i].a, b = edges[i].b, w = edges[i].w;

        a = find(a), b = find(b);
        if (a != b)     // 如果两个连通块不连通，则将这两个连通块合并
        {
            p[a] = b;
            res += w;
            cnt ++ ;
        }
    }

    if (cnt < n - 1) return INF;
    return res;
}
```

总而言之，Prim 选点，每次选出离树最近的点，Kruskal 选边，每次用最小的边合并并查集。Prim 适合稠密图，Kruskal 适合稀疏图

## Kruskal 重构树

[OI Wiki - Kruskal 重构树](https://oi-wiki.org/graph/mst/#kruskal-%E9%87%8D%E6%9E%84%E6%A0%91)

一种以 Kruskal 算法为基础的树形数据结构。在一个图中，它的构造如下：

- 将所有点视为集合，边为集合根节点之间的边
- 从小到大选择边，每次选边后：
  - 端点在同一集合则跳过，否则合并集合
  - 新集合的根节点为新建的节点，且点权=边权

最后，N 个点的图会生成拥有 2N-1 个点的树，叶子节点为原图的点，非叶子节点为新建的点

Kruskal 重构树有如下性质：

- 原图中 $u$ 到 $v$ 所有路径中，“最大边权的最小值” = 重构树上 $LCA(u,v)$ 的点权
- 从 $u$ 出发，只走边权 $\le x$ 的边能到的所有点 = 在重构树上凭证 $\le x$ 能跳到的最高祖先的整个子树里的所有叶子

