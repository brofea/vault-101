# 其他基础算法

本章内容：

- 前缀和 & 差分
- 双指针算法
- 位运算
- 离散化
- 区间合并

## 前缀和&差分

### 前缀和

若有数组 $a_0,a_1,a_2,a_3......a_i$，则 $a_i$ 数组的前缀和数组为 $s_i=\sum_{n=1}^{i} a_n$，对数组区间求和时使用 $\sum_{n=l}^{r} a_n=s_r-s_{l-1}$ 快速计算是前缀和的主要用法

需要注意的是，数组需要从`a[1]`开始存，前缀和数组首项`s[0]=0`，保证在求和时`l-1`不会越界

二维前缀和类似，用于快速求从某点到某点矩形区域内的所有元素的和

### 差分

差分是前缀和的逆运算，若有数组 $a_0,a_1,a_2,a_3......a_i$，构造 $b_i$ 使得 $a_i=\sum_{n=1}^{i} b_n$ 。用 $a_i$ 的差分 $b_i$ 存放数据时，若要对数组批量加减，时间复杂度会由 $O(n)$降低至$O(1)$，但访问 $a_i$ 的时间复杂度会从 $O(1)$ 提高至 $O(n)$，这是差分的主要用法

当需要给数组 `a[]` 区间 $[l,r]$ 内的元素加c时，差分数组的操作：`b[l] += c, b[r + 1] -= c;`

二维差分类似，构造差分矩阵后可以快速对矩形区域内的元素批量加减

二维前缀和例题一道：

> [落谷 - P1719 最大加权矩形](https://www.luogu.com.cn/problem/P1719)  

计算时：`pre[i][j]=pre[i-1][j]+pre[i][j-1]-pre[i-1][j-1]+a[i][j];`

取用时：`pre[k][l]-pre[i-1][l]-pre[k][j-1]+pre[i-1][j-1]`

## 双指针

> [OI Wiki - 双指针](https://oi-wiki.org/misc/two-pointer/)  

第一类：两个指针分别指向两个序列，维护某种次序，比如归并排序中合并两个有序序列的操作

第二类：两个指针共同指向一个序列，用两个指针维护一段区间

```cpp
for (int i = 0, j = 0; i < n; i ++ )
{
    while (j < i && check(i, j)) j ++ ;

    // 具体问题的逻辑
}
```

### 例题3

> [落谷 - P1147 连续自然数和](https://www.luogu.com.cn/problem/P1147)  

题解：

> [落谷 - 题解 P1147【连续自然数和】](https://www.luogu.com.cn/article/gra5qqiv)  

```cpp
#include <iostream>
using namespace std;
int main() {
    int m;
    cin >> m;
    int l=0,r=1;
    int total=1;
    while(l<r){
        if(total<m)
            r++,total+=r;
        else if(total>m)
            total-=l,l++;
        else{
            cout << l << " " << r << endl;
            total-=l,l++;
        }
    }
}
```

本题逻辑较为简单，不做过多赘述

## 位运算常用操作

### 问题1

求 n 的二进制表示中第 k+1 位是几。先将 n 右移 k 位，然后检查最低位

则 n 的第 k 位是 `n>>k&1`

应用：二进制转换

```cpp
#include <iostream>
using namespace std;
int main() {
    int n;
    cin>>n;
    for(int i=10;i>=0;i--)
        cout<<(n>>i&1);
    cout<<endl;
}
```

### 问题2

`lowbit(x)` 返回 x 的最后一位的 1 和后面的 0，若 x 是1010110000，则返回10000

本质上相当于 `x=x&-x` 相当于 `x=x&(~x+1)`

- 原码`x` 反码`~x` 补码`~x+1`

应用：求二进制数中1的个数

```cpp
#include <iostream>
using namespace std;
int lowbit(int x) {
    return x&-x;
}
int main() {
    int n,res=0;
    cin>>n;
    while(n) n-=lowbit(n),res++;
    cout<<res<<endl;
}
```

## 整数离散化

如果有数量不多但是数据范围很大的数字，将其离散化便是按序将其映射到一个下标，离散化后需要使用分求出对应数字的下标，以下模板包含了离散化和查找下标两种操作

![[image.png]]

```cpp
// 离散化数组
vector<int> alls; // 存储所有待离散化的值
sort(alls.begin(), alls.end()); // 将所有值排序
alls.erase(unique(alls.begin(), alls.end()), alls.end());   // 去掉重复元素

// 二分求出x对应的离散化的值
int find(int x) // 找到第一个大于等于x的位置
{
    int l = 0, r = alls.size() - 1;
    while (l < r)
    {
        int mid = l + r >> 1;
        if (alls[mid] >= x) r = mid;
        else l = mid + 1;
    }
    return r + 1; // 映射到1, 2, ...n
}
```

注意去重的实现`alls.erase(unique(alls.begin(), alls.end()), alls.end());`

函数`unique()`可以把数组中所有重复元素放在数组的末尾，然后返回这些重复元素的开头，最后用`erase()`，抹去所有重复元素

如 1 2 2 3 3 3 4 4 4 4 在经过函数`unique()`操作后变为 1 2 3 4 2 3 3 4 4 4

## 区间合并

将有交集的区间进行合并的算法，基本步骤如下：

1. 按左端点排序区间

1. 每次维护一段区间，将结尾标记放在初始区间的结尾，若下一个区间的开始小于标记，则将结尾更更新为该区间的结尾

![[image 1.png]]

```cpp
// 将所有存在交集的区间合并
void merge(vector<PII> &segs)
{
    vector<PII> res;

    sort(segs.begin(), segs.end());

    int st = -2e9, ed = -2e9;
    for (auto seg : segs)
        if (ed < seg.first)
        {
            if (st != -2e9) res.push_back({st, ed});
            st = seg.first, ed = seg.second;
        }
        else ed = max(ed, seg.second);

    if (st != -2e9) res.push_back({st, ed});

    segs = res;
}
```

### 例题4

> [落谷 - P1496 火烧赤壁](https://www.luogu.com.cn/problem/P1496)  

```cpp
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
const int N=2e4+5;
struct sec{int h,t;} a[N];
bool cmp(sec a,sec b){return a.h<b.h;}
int main() {
  int n;
  cin>>n;
  for(int i=0;i<n;i++)
    cin>>a[i].h>>a[i].t;
  sort(a,a+n,cmp);
  int hh=a[0].h,tt=a[0].t;
  vector<sec> b;
  for(int i=1;i<n;i++){
    if(a[i].h<=tt)
      tt=max(tt,a[i].t);
    else{
      sec tmp={hh,tt};
      b.push_back(tmp);
      hh=a[i].h;
      tt=a[i].t;
    }
  }
  sec tmp={hh,tt};
  b.push_back(tmp);
  int ans=0;
  for(int i=0;i<b.size();i++)
    ans+=b[i].t-b[i].h;
  cout<<ans<<endl;
}
```

区间 a 合并的结果为 b，最后统计 b 中每一段的差值
