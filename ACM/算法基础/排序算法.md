# 排序算法

## 快速排序

> [OI Wiki - 快速排序](https://oi-wiki.org/basic/quick-sort/)  

### 快排步骤

1. 确定分界点`x`，可以取`a` 数组在`l`、`r`、`(l+r)/2`的位置，其中`(l+r)/2`可以用`(l+r)>>1`实现

1. 升序情况下，将小于`x`的数放在`x`的左边，将大于`x`的数放在`x`右边

1. 递归运算`x`的左右两个区间

### 区间调整，暴力法

1. 开两个数组存放小于等于`x`的数和大于`x`的数

1. 将两个数组依次放入`a`

- 此方法不太会增加时间复杂度，但增加了空间复杂度

### 区间调整，指针法

1. 让`i`从左往右查找到第一个大于等于`x`的数，让`j`从左往右查找到第一个小于等于`x`的数

1. 交换`a[i]`和`a[j]`

1. 重复直到`i < j`

- 此方法保证`i`左边的数都要小于`x` ，`j` 右边的数都要大于`x`，但是最后一次循环`i`和`j`可能不会相邻，所以要从`j`和`j+1`分割进行递归，以保证右边全大于`x` 而左侧全小于`x`

```cpp
void quick_sort(int q[], int l, int r)
{
    if (l >= r) return;

    int i = l - 1, j = r + 1, x = q[(l + r) >> 1];
    while (i < j)
    {
        do i ++ ; while (q[i] < x);
        do j -- ; while (q[j] > x);
        if (i < j) swap(q[i], q[j]);
    }
    quick_sort(q, l, j), quick_sort(q, j + 1, r);
}
```

其中`do while` 语句可以用`while` 语句进行替换：

```cpp
while(a[++i]<x);
while(a[--j]>x);
```

当使用`(l, j)`和`(j+1, r)` 作为下一步递归区间时，`x` 可以为`(l+r)>>1` 或`l` ，但是不能为`r` ，同理，当使用`(l, i-1)`和`(i, r)` 作为下一步递归区间时，`x` 可以为`(l+r+1)>>1` 或`r` ，但是不能为`l` ，否则程序可能死循环

### 例题1

> [落谷 - P1177 【模板】排序](https://www.luogu.com.cn/problem/P1177)  

```cpp
#include <iostream>
#include <cstdio>
using namespace std;
const int N=1e5+10;
void quicksort(int a[],int l,int r){
    if(l>=r)
        return;
    int i=l-1,j=r+1,x=a[(l+r)>>1];
    while(i<j){
        while(a[++i]<x);
        while(a[--j]>x);
        if(i<j) swap(a[i],a[j]);
    }
    quicksort(a,l,j);
    quicksort(a,j+1,r);
}
int main(){
    int n;
    int a[N];
    ios::sync_with_stdio(false);
    cin>>n;
    for(int i=0;i<n;i++)
        cin>>a[i];
    quicksort(a,0,n-1);
    for(int i=0;i<n;i++)
        cout<<a[i]<<' ';
}
```

## 归并排序

> [OI Wiki - 归并排序](https://oi-wiki.org/basic/merge-sort/)  

- 归并排序是稳定的。在原序列中若有两个相同的数，在排序后他们的相对位置不会发生改变，则称这个排序算法是稳定的

### 归并步骤

1. 定义`mid`赋值为`(l+r)>>1`将数组从中间划分为两个数组并递归

1. 递归至`l==r`返回，此时区间内仅有一个变量

1. 递归返回，合并数组

### 合并数组的步骤

1. 使用两个指针`i`、`j`指向两半数组的开头`l`、`mid+1`

1. 循环比较更小的值放入临时数组`tmp`中，更小的值所在数组的指针向后移动一位

1. 循环在指针`i`或`j`走到末尾时停止，即`i==mid`和`j==r`

1. 将未取出所有值的数组剩下的所有值写入`tmp`数组的尾部，模板中并没有额外判断是哪个数组走到了尾，所以直接使用了两个`while`

1. 将`tmp`数组的所有值写入回原数组，注意原数组需要从`l`开始循环，而`tmp`数组从`0` 开始循环

- 用于合并的两个数组都是递增的，所以指针指向的永远是数组未被放进`tmp`数组中最小的一个

- `k` 用于指向tmp数组的末尾，每次使用`i`、`j`、`k`的时候都要注意++

- 在语句`if (q[i] <= q[j])`中使用`<=` 而不是`<`保证了相同大小的数总是先放入`mid`左侧数组，因此保证了归并排序的稳定性

```cpp
void merge_sort(int q[], int l, int r)
{
    if (l >= r) return;

    int mid = (l + r) >> 1;
    merge_sort(q, l, mid);
    merge_sort(q, mid + 1, r);

    int k = 0, i = l, j = mid + 1;
    while (i <= mid && j <= r)
        if (q[i] <= q[j]) tmp[k ++ ] = q[i ++ ];
        else tmp[k ++ ] = q[j ++ ];

    while (i <= mid) tmp[k ++ ] = q[i ++ ];
    while (j <= r) tmp[k ++ ] = q[j ++ ];

    for (i = l, j = 0; i <= r; i ++, j ++ ) q[i] = tmp[j];
}
```

- 使用上述模板时需要定义与`q[]`等大的 `tmp[]`

### 例题2

> [落谷 - P1177 【模板】排序](https://www.luogu.com.cn/problem/P1177)  

```cpp
#include <iostream>
#include <cstdio>
using namespace std;
const int N=1e5+10;
int tmp[N];
void mergesort(int a[],int l,int r){
    if(l>=r) return;
    int mid=(l+r)>>1;
    mergesort(a,l,mid);
    mergesort(a,mid+1,r);
    int k=0,i=l,j=mid+1;
    while(i<=mid && j<=r){
        if(a[i]<=a[j])
            tmp[k++]=a[i++];
        else
            tmp[k++]=a[j++];
    }
    while(i<=mid)
        tmp[k++]=a[i++];
    while(j<=r)
        tmp[k++]=a[j++];
    for(i=l,j=0;i<=r;i++,j++) 
        a[i]=tmp[j];
}
int main(){
    int n;
    int a[N];
    ios::sync_with_stdio(false);
    cin>>n;
    for(int i=0;i<n;i++)
        cin>>a[i];
    mergesort(a,0,n-1);
    for(int i=0;i<n;i++)
        cout<<a[i]<<' ';
}
```
