---
标签:
  - 数据结构
---

> [OI Wiki 并查集](https://oi-wiki.org/ds/dsu/)  

基础知识不再赘述，实现参考下列模板

```C++
int p[N]; //存储每个点的祖宗节点

// 返回x的祖宗节点
int find(int x) {
  return p[x];
}

// 初始化，假定节点编号是1~n
for (int i = 1; i <= n; i ++ ) p[i] = i;

// 合并a和b所在的两个集合：
p[find(a)] = find(b);
```

# 额外类型

在基础的并查集上，可以维护两个信息

- 集合中节点个数

- 当前节点到根节点的距离

# 启发式合并

## 按秩合并

常用的办法，每次都选择高度较少的一棵树连接到另外一棵树，避免发生退化

```C++
int p[N]; // p[i] 表示 i 的父节点
int rk[N];  // rk[i] 是以 i 为根的树的近似高度（秩）

// 初始化 n 个元素，每个元素自成一个集合
void dsu_init(int n) {
  for (int i = 1; i <= n; ++i) {
    p[i] = i; // 每个节点的父节点初始是自己
    rk[i] = 0;  // 初始秩为 0
  }
}

// 查找 x 所在集合的代表元素（根节点）
int dsu_find(int x) {
	// 路径压缩：使 x 直接连到根
  if (p[x] != x) p[x] = dsu_find(p[x]); 
  return p[x];
}

// 合并 x 和 y 所在的集合，按秩合并
bool dsu_union(int x, int y) {
  int rx = dsu_find(x), ry = dsu_find(y);
  if (rx == ry)
    return false; // 已在同一集合，无需合并

  // 按秩合并：小树挂到大树下面
  if (rk[rx] < rk[ry]) {
    p[rx] = ry;
  } else if (rk[rx] > rk[ry]) {
    p[ry] = rx;
  } else {
    p[ry] = rx;
    rk[rx]++; // 两棵一样高的树合并，高度+1
  }
  return true; // 成功合并
}
```

按秩合并 + 路径压缩的并查集时间复杂度趋于 $O(\alpha(n))$，其中 $\alpha$ 是阿克曼函数的反函数，阿克曼函数 $A(m,n)$ 是一种增长速度极快的函数，当 $m=4$ 时增长速度就超过指数级增长，所以反函数的时间复杂度极低，在竞赛中 $\alpha(n)$ 几乎不会超过 5，所以上述算法的时间复杂度可看作是线性的

## 按大小合并

每次都选择节点数量较少的一棵树连接到另外一棵树，避免发生退化

在需要集合大小的问题上，适合使用这个，时间复杂度和按秩合并类似

```C++
int parent[MAXN], size[MAXN];

void dsu_init(int n) {
    for (int i = 1; i <= n; ++i) {
        parent[i] = i;
        size[i] = 1; // 每个集合初始大小为1
    }
}

int dsu_find(int x) {
    return parent[x] == x ? x : parent[x] = dsu_find(parent[x]);
}

bool dsu_union(int x, int y) {
    int rx = dsu_find(x), ry = dsu_find(y);
    if (rx == ry) return false;

    // 小集合挂到大集合
    if (size[rx] < size[ry]) {
        parent[rx] = ry;
        size[ry] += size[rx];
    } else {
        parent[ry] = rx;
        size[rx] += size[ry];
    }
    return true;
}
```

# 基础例题

## 并查集判断环


> [信息学奥赛一本通 - 格子游戏](http://ybt.ssoier.cn:8088/problem_show.php?pid=1347)  

AC 代码

```C++
#include <bits/stdc++.h>
using namespace std;
const int N = 210 * 210;
int n, m, p[N];
int find(int x) {
  if (p[x] != x) p[x] = find(p[x]);
  return p[x];
}
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cin >> n >> m;
  for (int i = 0; i < N; i++) p[i] = i;
  int x, y, u, nu, pu, pnu; char dir;
  for (int i = 1; i <= m; i++) {
    cin >> x >> y >> dir;
    u = (x - 1) * n + y;
    nu = u + (dir == 'R' ? 1 : n);
    pu = find(u), pnu = find(nu);
    if (pu == pnu) {
      cout << i << endl;
      return 0;
    }
    p[nu] = pu;
  }
  cout << "draw\n";
  return 0;
}
```

## 并查集 + 01背包

- 集合内的点权可以直接累加在根节点上，不需要新开数组

> [洛谷 - P1455 搭配购买 - 洛谷](https://www.luogu.com.cn/problem/P1455)

## 并查集 + 哈希

- 先考虑相等的约束条件再考虑不等的约束条件

> [洛谷 - P1955 [NOI2015] 程序自动分析](https://www.luogu.com.cn/problem/P1955)  

## 状态维护

> [洛谷 - P1196 [NOI2002] 银河英雄传说](https://www.luogu.com.cn/problem/P1196)  

## 带权并查集

> [洛谷 - P5937 [CEOI 1999] Parity Game](https://www.luogu.com.cn/problem/P5937)  

> [洛谷 - P2024 [NOI2001] 食物链](https://www.luogu.com.cn/problem/P2024)